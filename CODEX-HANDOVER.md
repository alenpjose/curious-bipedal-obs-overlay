# Codex handover

Audit this repository as a Windows x64 native OBS plugin targeting OBS Studio 32.1.2, Aitum Vertical, and Aitum Multistream. Verify API use, lifetime/thread safety, source-instance isolation, logo safe padding, rendering at 2560×1440 and 1440×2560, main OBS timer events, and Aitum Vertical output start/stop binding. Fix all build, runtime, packaging, and documentation issues you find. Run the Windows GitHub Actions build, inspect failures, produce and smoke-test the DLL, portable ZIP, and Inno Setup installer in OBS 32.1.2 with Aitum installed. Confirm two new source instances keep separate settings and timers. Then commit intentionally, push to `https://github.com/alenpjose/curious-bipedal-obs-overlay`, and open a draft PR (or push `main` only if the user explicitly requests it). Do not claim the installer is validated until the real OBS/Aitum smoke test passes.
